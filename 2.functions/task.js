// Задание 1
function getArrayParams(arr) {
  let min = arr[0];
  let max = arr[0];
  let sum = 0;
  

  for (i = 0; i < arr.length; i++) {
    arr[i] > max => max = arr[i];
    arr[i] < min => min = arr[i];
    sum += arr[i];

    avg += sum/arr.length.toFixed(2); 

    let num = Number(avg);


  return { min: min, max: max, num: num };
}


// Задание 2
function worker(arr) {
  let sum;

  // Ваш код

  return sum;
}

function makeWork(arrOfArr, func) {
  let max;

  // Ваш кода
  // for ...
  
  return max;
}

// Задание 3
function worker2(arr) {
  // Ваш код
}
